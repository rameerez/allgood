<style>
  header {
    min-height: 20vh;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    background-color: <%= @status == "ok" ? "Chartreuse" : "FireBrick" %>;
    font-weight: bold;
    font-size: 2.5rem;
    margin-bottom: 1em;
  }

  .check {
    margin: 0.5em 0;
  }
</style>

<header>
  <h1><%= @status == "ok" ? "🤙 It's all good" : "❌ Something's wrong" %></h1>
</header>

<% if @results.any? %>
  <% @results.each do |result| %>
    <div class="check">
      <%= result[:success] ? "✅" : "❌" %>
      <b><%= result[:name] %></b>: <i><%= result[:message] %></i> <code>[<%= result[:duration] %>ms]</code>
    </div>
  <% end %>
<% else %>
  <p>No health checks were run. Please check your configuration.</p>
<% end %>